# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database (Supabase = PostgreSQL in production, SQLite for local dev) ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
supabase==2.7.4

# --- Auth ---
python-jose[cryptography]==3.3.0
bcrypt==4.2.0

# --- AI / Computer Vision ---
opencv-python-headless==4.10.0.84
mediapipe==0.10.14
numpy==1.26.4
pillow==10.4.0

# --- AI / Machine Learning (recommendation) ---
xgboost==2.1.1
scikit-learn==1.5.2
pandas==2.2.3
joblib==1.4.2

# --- LINE Messaging API ---
line-bot-sdk==3.11.0

# --- misc ---
httpx==0.27.2
python-dotenv==1.0.1

# --- optional: server-side YOLOv8-Seg fine-tuning / inference ---
# หนักและใช้เวลาติดตั้งนาน (ต้องใช้ torch) ให้ติดตั้งแยกเฉพาะตอนจะเทรน/ใช้โมเดล YOLOv8-Seg จริงเท่านั้น
# pip install ultralytics==8.2.103
