# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database (Supabase = PostgreSQL in production, SQLite for local dev) ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
supabase==2.7.4

# --- Auth ---
python-jose[cryptography]==3.3.0
bcrypt==4.2.0

# --- AI / Computer Vision ---
opencv-python-headless==4.10.0.84
mediapipe==0.10.14
numpy==1.26.4
pillow==10.4.0

# --- AI / Machine Learning (recommendation) ---
xgboost==2.1.1
scikit-learn==1.5.2
pandas==2.2.3
joblib==1.4.2

# --- LINE Messaging API ---
line-bot-sdk==3.11.0

# --- Scheduled jobs (แจ้งเตือนลูกค้าก่อนถึงคิว 1 วัน) ---
apscheduler==3.10.4

# --- misc ---
httpx==0.27.2
python-dotenv==1.0.1
slowapi==0.1.9   # จำกัดจำนวนครั้งเรียก API ต่อ IP กัน brute-force login + สแปม

# --- server-side YOLOv8-Seg inference (เพื่อนเทรนโมเดลเสร็จแล้ว วางไว้ที่ ml/models/yolov8_nail_seg.pt) ---
# ultralytics พ่วง torch มาด้วย -- ปกติ pip จะดึงเวอร์ชันที่รวม CUDA/GPU มาให้ (หนักเป็น GB เพราะลาก
# ไลบรารี nvidia-* มาเพียบ) ทั้งที่ Render ไม่มี GPU ใช้เลย จึงบังคับดึงจาก index ของ PyTorch เองที่มี
# เฉพาะ CPU (+cpu) แทน เบากว่ามาก (~150-200MB) -- ยังไม่เคย build จริงบน Render มาก่อน ถ้า deploy แล้วมี
# ปัญหาเรื่องพื้นที่/แรมไม่พอบนแผนฟรี ต้องพิจารณาอัปเกรดแผนหรือหาโมเดลที่เบากว่า -- ระบบยัง fallback ไป
# mediapipe+opencv ได้เองถ้าโหลดโมเดลนี้ไม่สำเร็จ (ดู segmentation.py segment_nails())
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
ultralytics==8.2.103
